# 复制此文件为 .env，并填入实际值
# cp .env.example .env

# SQLAlchemy 数据库连接串
# SQLite（本地开发默认）：
DATABASE_URL=sqlite:///./visits.db

# PostgreSQL 示例（生产环境可替换）：
# DATABASE_URL=postgresql://user:password@localhost:5432/visits_db
